- model_class = Representative
.page-header
  %h2=t '.title', :default => model_class.model_name.human.pluralize.titleize

= panel t("views.titles.search_filters", default: "Filters"), "reprensentatives-search-form" do  
  = datagrid_form_for @grid, :method => :get, :url => representatives_path, clear_url: representatives_path, html: { class: "form-horizontal" }

#representatives-search-results
  = render "grid"
  
:javascript
  $(".panel-collapse").trigger("click")