- if can? :delete, Visit
  = button_tag "Delete selected", type: :submit, name: "delete_selected", class: "btn btn-danger btn-xs", data: { confirm: t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, rel: "tooltip", title: t("mongoid.actions.customer.delete", default: t("helpers.links.delete"))
  = button_tag "Delete all", type: :submit, name: "delete_all", class: "btn btn-danger btn-xs", data: { confirm: t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, rel: "tooltip", title: t("mongoid.actions.customer.delete", default: t("helpers.links.delete"))